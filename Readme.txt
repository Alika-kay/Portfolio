The document contains a HTML, CSS and JS files, the HTML contains the structure,
the CSS file contains the styling for the HTML and the JS is used for the animation.

The document is edited and created by Alika Kayembe admission 136304.